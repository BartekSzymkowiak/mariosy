volumes:
  keycloak_data:

services:
  keycloak:
    image: quay.io/keycloak/keycloak:22.0.1
    env_file:
      - .keycloak.env
    ports:
      - "8080:8080"
    volumes:
      - keycloak_data:/opt/keycloak/data/
    command:
      - start-dev
